# Dataset Statistics

The Dataset Statistics module (`statistics.py`) automatically generates Markdown and JSON reports for any given dataset distribution.

It tracks:
- Total images
- Class Distribution (e.g. positive vs negative).
- Bounding Box Counts
- Annotator and Reviewer Participation

These reports are injected directly into the QAPipeline for final validation before a Dataset Release is minted.
